<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apiandroidtask">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".DI.App"
        android:allowBackup="true"
        android:banner="@drawable/ic_coincap"
        android:icon="@drawable/ic_coincap"
        android:label="CoinCap"
        android:logo="@drawable/ic_coincap"
        android:roundIcon="@drawable/ic_coincap"
        android:supportsRtl="true"
        android:theme="@style/Theme.APIAndroidTask">
        <activity android:name=".AdditionalInfoActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>